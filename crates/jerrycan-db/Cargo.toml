[package]
name = "jerrycan-db"
description = "Database extension (SQLite/Postgres, migrations) for the jerrycan framework. Name reservation; real releases begin at 0.1.0. https://jerrycan.cc"
version.workspace = true
edition.workspace = true
license.workspace = true
homepage.workspace = true

[dependencies]
